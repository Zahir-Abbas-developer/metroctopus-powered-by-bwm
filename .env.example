# Development database. SQLite locally; swap for a Postgres connection string
# (Neon/Supabase) at deploy time along with the provider in prisma/schema.prisma.
DATABASE_URL="file:./dev.db"

# NextAuth. Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"
